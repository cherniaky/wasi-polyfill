# Custom `fd_write` polyfill

## Quick Start

```console
$ rustc --target wasm32-wasi main.rs
$ python3 -m http.server 6969
<open http://localhost:6969/>
<open DevTools Console and observe the output>
```
